## Quickstart (CLI)

### Prereqs
- Docker Desktop (or equivalent)
- Flutter SDK (host) if you want to run macOS desktop locally

### Setup
```bash
make setup
make doctor
make test
```

### Run Flutter web in Docker
```bash
make run-web
```

### Run MacOS desktop (host)
```bash
cd app
flutter run -d macos
```
